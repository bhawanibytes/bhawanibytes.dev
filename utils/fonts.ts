import { Inter, Signika, Raleway } from "next/font/google";

export const raleway = Raleway({ subsets: ["latin"] });
export const inter = Inter({ subsets: ["latin"] });
export const signika = Signika({ subsets: ["latin"] });
